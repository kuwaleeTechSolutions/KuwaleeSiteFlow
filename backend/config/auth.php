<?php

return [
    // 'web' guard uses the session driver — this is what makes Sanctum's
    // cookie-based SPA authentication work: the session cookie identifies
    // the logged-in user, no bearer token is ever issued to the SPA.
    'defaults' => [
        'guard' => 'web',
        'passwords' => 'users',
    ],

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],
        // Reserved for any future machine-to-machine / mobile-app token use
        // case that genuinely requires bearer tokens instead of cookies.
        'sanctum' => [
            'driver' => 'sanctum',
            'provider' => 'users',
        ],
    ],

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => App\Models\User::class,
        ],
    ],

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    'password_timeout' => 10800,
];
